I need to create a well defined stored prompt to verify implementation agains analysis files. As an example I provide an analysis describing what to do to implement a feature (add client entity). It is expedted that the analysis will be provided when the prompt will be invoked and passed to AI.

The analysis may consist of task definitions for developers, testers, or operations. It can include intentions, steps and possibly architectural decisions. In the prompt, there shall be instructions to examine the code base with focus on actual delivered code. The features will be delivered in commits in git branches named "feature/${id-of-feature}" and all commits relevant to the feature will have commit message starting with "${id-of-feature}:". Check the feature branch first, then try to find commits in main branches, i.e. those without folder prefix. Use capability of executing git to fetch the actual changes from the repository to get exactly the code touched in the process of delivering the feature. Check also that no other actions happened in the commits.